import {AppRegistry} from 'react-native';
import App from './App';
import StackNavigator from 'react-navigation';

console.ignoredYellowBox = ['Remote debugger'];

AppRegistry.registerComponent('work', () => App);
